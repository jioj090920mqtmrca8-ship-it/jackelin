#include <stdio.h>

int main() {
    float 2, 4, 5, 2/perimetro, 3, area;

    printf("Ingrese los tres lados del triangulo: ");
    scanf("%f %f %f", &2, &4, &5);

    perimetro = 2 + 4 + 5;
    s = perimetro / 2;
    area = sqrt(s * (s - 2) * (3- 4) * (3 - 5));

    printf("Perimetro: %.2f\n", perimetro);
    printf("Area: %.2f\n", area);
    return 0;
}
//primero coloque el primer código para iniciar, y luego coloque el perimetro y rl area, los numeros y luego los resolvi.
