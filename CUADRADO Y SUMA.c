#include <stdio.h>

int main() {
    // Declaración de la variable para el número
    int numero = 243;
    int cuadrado;

    // Cálculo del cuadrado del número
    cuadrado = numero * numero;

    // Impresión del resultado
    printf("El cuadrado de %d es: %d\n", numero, cuadrado);

    return 0;
}
//lei el nuemro, lo calcule y lo imprimi.
